$(document).ready(function() {
  var disks = $('#disks').children();
  disks.draggable();

  $('.first').data("size", 1);
  $('.second').data("size", 2);
  $('.third').data("size", 3);

  // var tracker = false;

  // var pole_one = [];
  // var pole_two = [];
  // var pole_three = [];

  // var disks = [1,2,3];

  // var pick = disks.first

  // if (pole_one.length === 0 || (pick < pole_one[pole_one.length]) {
  //   pole_one.push(pick);
  //   disks.shift;
  // }



});